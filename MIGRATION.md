# 单仓库迁移说明

## v0.2 候选版：格式 6

首次打开格式 4/5 的库时，先保存 SQLite 备份，再迁移到格式 6。新表保存单篇 chat、旧会话入口、Figure 上下文、研究字段、显示方案和雷达候选；原论文、PDF、解析、译文、Reader 及笔记保留。迁移可重复执行。

每篇论文通过稳定 paperId 绑定一个原生 sessionId。移动或重命名分类继续原 chat；归档分类中的论文暂停受限操作。旧分类会话作为只读历史保留，不把混合讨论重新归属到一篇论文。旧单篇 Review 的关系继续可读。

Excel 升级为 v3 同步合同，按稳定字段 ID 区分显示列名与数据身份，保留既有个人记录。旧表先备份，冲突不覆盖。请在工作台修改表头显示名；直接在 Excel 改表头会停止回写并保留原表。

升级前关闭旧实例并备份完整数据目录。格式 6 不能直接交给旧引擎写入；回退旧版本须在独立目录恢复升级前的完整备份，保留升级后的数据供后续迁移。详见 [v0.2 指南](docs/v0.2-guide.md)。

从本版本开始，只需克隆 `dsh-scientific-reading`。`sr_setup` 会把 npm 包内置的 Python wheel 安装到数据目录 `.venv`。

旧的 `Scientific-Reading-for-Newbies` checkout 不再参与运行，可以在新版本验收后自行归档。插件不会移动或删除旧 PDF、generation、HTML 或用户字段；旧执行命令与旧 Zotero/快速解析流程不再提供。

## Excel 长期管理（rc.6）

数据库升级到格式 5，增加阅读进度、课题关联、下一步、个人维护时间及 Excel 导出基线。首次访问旧库时先备份再迁移；原有三列个人记录保留。迁移后请继续使用支持格式 5 的程序，不直接切回旧引擎。

首次刷新旧表前，程序保存原始工作簿到数据目录的 `backups/xlsx/`。旧表中的个人理解程度、个人思考、用户笔记可导入；旧会话列和其他原始内容留在备份中。若旧表与库内已有笔记不同，保留两边并返回冲突，不静默覆盖。

新表包含文献、阅读成果、图表索引和说明。文献页黄色六列允许回写，其余字段由系统维护；请整表排序，保存关闭后刷新。不同副本和并发修改按导出基线核对，冲突时原工作簿保持不变。完整迁移或换机请备份整个文献库，不能仅复制 `.xlsx`。
