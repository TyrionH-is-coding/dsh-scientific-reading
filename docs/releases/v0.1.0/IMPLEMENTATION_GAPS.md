# 0.1 实现与验收状态

更新：2026-09-06。产品范围沿用[双项目架构](../../superpowers/specs/2026-09-05-two-product-literature-architecture.md)。本表替代早期“B 尚未创建”的开工清单。

## A：原生插件

| 项目 | 实现与当前证据 |
| --- | --- |
| OA 与手动 PDF 边界 | 已实现。只加载固定 ScanSci OA 模块，跳过会修改网络环境或导入机构来源的聚合入口；Europe PMC 仅显式 OA，Unpaywall 仅显式邮箱。真实 arXiv PDF 已验；Europe PMC 全文超时正确转待补 PDF |
| 设置与状态 | 已实现并在 B 内真实浏览器验证：MinerU Key、只读资产位置、本地库与 OA/解析能力；原生模型配置保留，保存 Key 与真实调用分开 |
| 入库、去重与摘要 | SQLite 本地事务、后台题录/摘要派生、缺失明确保留，现有完整回归覆盖 |
| 全文任务与资产 | parent job、PDF SHA、分阶段恢复、Reader/导出 manifest；重复补 PDF 续接原任务已在真实 B 安装包验证 |
| Excel、备份恢复 | 固定列、白名单用户字段回写、一致性备份与恢复合同已有回归；B 真正迁移安装后 PDF/Reader 已读回。当前机器没有打开 Excel GUI，不将其记为本轮 GUI 验收 |
| 分类范围支持 | 查询、任务、Review 与最终发布校验；移动/归档后旧任务拒绝写回，A 回归通过，B 真实宿主越界检查通过 |
| 内容显示 | R1 已定位的公式转义、引用续段、重复页眉和字符显示缺陷已修复；同源重渲染及 Edge 实际交互通过。该样本原 OCR 数字/DOI/图注分段仍有明确限制，继续为非正式工程预览 |
| A 独立安装 | 最终 `15f752…` 包在全新托管 venv 的实际 sr_setup、OA probe、实际 CLI 调用均通过；未指定外部引擎或 ScanSci 解释器 |

已完成第一轮 A 全回归：378 passed、1 skipped，40 项 offline、4 项 assets，重启恢复 4 个中断点。此结果对应第一份 rc.1 SHA；R1 修复后的最终包会另记，不继承旧包通过结论。

最终 R1 修复包重新通过 `npm test`：386 passed、1 skipped，全部 offline、4 项 assets。57 个 tarball 文件与 wheel 内 49 个 Python 源文件均逐字节回读一致。固定 SHA-256 为 `15f752f359ed2865a32671e1c7e0240bb74ad22033b706029efcbc7110672c59`，见[可公开验收摘要](ACCEPTANCE.md)。原始修复和同包安装报告保留在本机。

## B：Codex 独立工作台

| 编号 | 0.1 项目 | 当前状态 |
| --- | --- | --- |
| PB-01 | 独立项目、固定依赖安装 | 已实现；私有 Node 22.22.2、Python 3.11.16、DSH rc.7、A tarball、OAuth CLI 与依赖 hash 锁定。PowerShell 5.1 的全新安装和迁移安装通过 |
| PB-02 | 完整实例隔离 | 已实现并实测。独立 DSH_HOME、CODEX_HOME、npm/venv、库和会话；513 个模块链接在实例内；外部配置和凭据不继承 |
| PB-03 | 启动、停止、实例身份 | 已实现并实测。随机回环端口、并发启动复用、停止/重启可观察，3080 并存哨兵不受影响 |
| PB-04 | Codex Skill 与内置浏览器 | 已实现。Skill 安装与冲突保留有测试，HTML 实例页、DSH 会话、设置页、OAuth 页与迁移 Reader 已在内置浏览器核对 |
| PB-05 | 机器交接 | 已实现并实测。稳定请求键、任务和 RPC 回执；完成依据 A 状态及真实 Reader HTTP SHA；重复 PDF 补入只续接同一任务 |
| PB-06 | 两级持久管理员 | 已实现并实测。两个分类两条原生会话，改名不丢绑定；本地验收模型真正执行两轮 DSH 工具调用，重启后保留会话和模型选择 |
| PB-07 | 分类执行范围 | 已实现并实测。无分类参数查询仍受限，外部任务/论文/逃逸工具被拒绝，移动和归档使旧任务失效；最终安装包真实原生子会话继承查询和写入范围 |
| PB-08 | Codex 订阅入口 | 已实现，固定社区适配器来源与许可，使用本实例官方 Codex CLI。28 项回归、真实未登录协议/取消/退出通过；本人登录、真实授权模型回合与刷新仍须单列验证 |
| PB-09 | 原生模型与额度 | 已实现。原生 provider/model 选择保留，账号缺失显示未登录/额度不可用；不静默转 API 计费；真实额度取决于本人登录 |
| PB-10 | 授权获取与手动兜底 | Skill 已接入合法获取边界，PDF 导入真实通过。机构访问由外层浏览器能力与用户权限决定，不能保证网络/验证码永不失败；不搬运 Chrome 登录文件 |
| PB-11 | 重启与换对话恢复 | 已实现并实测。SQLite、持久绑定、任务及 RPC 回执恢复；崩溃后冷会话取消只移除本任务队列；正常关闭撤销的消息从原生事件识别为 canceled，缺证据为 uncertain，同 gate 不盲目重发 |
| PB-12 | 升级、回退、卸载、迁移 | 已实现。坏候选启动失败恢复及切换中断恢复通过；迁移+Reader通过。最终包实际 PowerShell 5.1 升级/回退 9 项通过，17 条题录和 13 个 PDF/HTML/Excel 资产保留；最终卸载 30 项通过，49 库文件与工作区保留、修改 Skill 保留，自有未修改 Skill 和程序被移除 |

## 冻结结果

1. 已完成本轮明确的 R1 显示修复、新 A 全回归、实包读回及同包独立托管安装。
2. 独立审查提出的取消归属、冷会话恢复、Skill 冲突和正常关闭后的投递证据问题均已修复，B 37 项回归通过。
3. B 固定最终 A SHA 与新 lockfile。最终 zip SHA 为 `f49d07bbba15446ef728a4541febf5610893b6932bf0e98ba390328bfe844a54`；解压后 64 文件逐一校验。安装程序 SHA 为 `2b6ebcc81a86b9d355e06f9928e5383816b392d16fe207c695af95bad0165739`。
4. 发布附件与逐项验收放在兄弟项目 `codex-scientific-reading/outputs/v0.1.0-rc.1-delivery`。本轮交付为可安装的发布候选，不是已经公开的稳定版。
5. 本人 OAuth 登录/授权模型回合、最终组合的全新 MinerU 真实服务调用、Excel GUI 仍未覆盖。真实 R1 的工程显示已修，原 OCR 和科学内容仍有明确限制，不能用本地模型夹具或文件 SHA 代替内容验收。

## 保持在 0.2

自定义 Excel 展示列/研究字段、HTML/CSS 模板、文献雷达与主动方向推荐。首版不扩展为通用订阅聚合、任意 Excel 双向同步、跨论文自动综述或通用向量库。
