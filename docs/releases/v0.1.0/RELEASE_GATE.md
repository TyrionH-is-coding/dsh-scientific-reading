# 公开放行修复记录

本记录跟进对 `a0db7cf` 的独立验收。该提交的 GitHub CI 失败；其 Release 曾公开，现已按用户决定撤回为草稿，不能作为公开候选放行。旧 tag `v0.1.0-rc.1` 暂保留在该提交，没有自动改写。

修复提交 `401e98256d42e10650b5ec71a475634c1aedfd1b` 的[完整 CI](https://github.com/TyrionH-is-coding/dsh-scientific-reading/actions/runs/34013006939)已经通过：构建、引擎 388 项、离线与资产测试均成功。后续文档收尾提交仍须核对其自身 CI，不能将本次结果自动延伸到未来提交。

## 修复

- 审核服务先原子写入并同步 `server.json`，再注册可返回成功的 HTTP handler。写入失败时关闭监听并以失败退出。
- 新回归用测试专属 preload 暂停实际文件 rename；修复前稳定观察到 `/health` 已成功但文件不存在，修复后在同一窗口不再宣称就绪。另测写入失败不会留下可用服务。没有修改 CLI 测试以容忍缺失文件。
- `design.md`、`features.md` 与 OA-only 合同一致；机构访问由独立 B 的外层授权浏览器处理。无生产引用的 `src/institution.ts` 删除。
- 发布说明使用已存在的源码指南链接，避免尚无 tag 时访问 404。
- 首次修复提交 `5f574a8` 的 CI 已通过审核服务及 CLI 回归，随后在资产夹具暴露 Windows 短路径与真实路径混用的问题。夹具入口统一 `resolve()`，并增加未解析根目录的实际样本生成回归；此修复不改插件运行代码或冻结附件。
- 资产检索测试的 Python 子进程显式使用 UTF-8，避免中文 JSON 在未预设 UTF-8 的 Windows 环境被错误解码。本机已先复现该失败再通过修复后的测试。

## 附件来源

按照保留同一冻结 tarball 的要求，A 附件仍为 SHA-256 `15f752f359ed2865a32671e1c7e0240bb74ad22033b706029efcbc7110672c59`。本轮完整测试会重新构建本机临时产物，但不能用这些新构建替换该附件。

冻结 A 包内仍有三个未被生产入口引用的历史机构编译文件（JS、source map 和类型声明），以及先前 README；新源码已移除对应 TS 文件并修正文档。审核服务属于仓库审核工具，不在该 tarball 的分发文件范围内。不得声称冻结包与新 tag 整树逐字节一致。

原附件生成时没有对应的已提交构建记录。公开清单分别记录本次通过 CI 的放行源码 SHA、原构建未提交的事实、原始源码基线和冻结附件 SHA，避免把一个后来创建的提交伪装为原始构建提交。只有 tag 与 prerelease 实际创建并读回后，才能报告已公开。

## 保留的验收边界

本人 OAuth、当前组合真实 MinerU 调用、Excel GUI 和科学内容全面验收继续单列；历史本机测试见 ACCEPTANCE.md。本轮是否通过以实际完整测试日志和放行提交的 CI 为准，不因为本记录存在就默认通过。B 的源码与安装包维持私有，不借 A 的公开 Release 对外分发。
