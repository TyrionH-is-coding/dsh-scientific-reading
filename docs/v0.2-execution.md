# v0.2 完整构建与验收记录

## rc.2：步骤模型、在线阅读助手与离线分享

更新：2026-09-09。当前候选为 **0.2.0-rc.2**；下方 rc.1 记录保留其原验收版本和 SHA。本次仍是隔离工作树快照，尚未提交、合并或正式发布。rc.1 交付目录保留。

11 个生成步骤可在设置页分别选择模型与思考深度：摘要及全文翻译默认 Luna，其余默认 Sol，全部默认 medium。模型列表与可用思考档位取自实际连接；不支持时提示修改，不静默降级。配置保存在既有 SQLite 元数据中，数据格式仍为 6。原生 chat 根据真实任务阶段选择模型；外部 Codex 管理员通过 `model_run` 调用步骤配置，外层 Codex 自身的模型由 Codex 控制。

在线 Reader 加入“问”悬浮球、选区解释（Alt+Shift+E）、流式回答、追问、停止和来源跳转。选区与 PDF 身份由服务器复核；选择译文时带上对应原文和邻段。无选区只提供摘要和相关段落，不能声称完整读过全文。悬浮对话只处理文字，图像讨论沿用 Figure 入口。

离线导出为包含正文、双语内容、导读与内嵌图片的单个 HTML；适配移动宽度，禁止网络请求，不含本页聊天记录、模型配置或登录信息。“在线”指连接正在运行的工作台，未部署公网服务；即时悬浮对话不保证刷新或重启后保留。

本次验证：

- A 完整 Python：552 passed、3 skipped；完整 test:offline、test:assets、构建与类型检查通过。
- A 原生 DSH 与浏览器：16 项通过。实际下拉保存后请求使用选择的模型/深度；含选区、追问、伪造选区拒绝、390px 无溢出、离线图片完整及零 HTTP 请求。
- B 完整 Node：82 passed、1 skipped。真实安装器将隔离旧实例升级到 rc.2，已安装代码原生验证 10 项通过，包括外部调用切换模型、重启保留设置、已有 Reader 在线/离线入口及测试配置恢复。
- A 内置 wheel 的 62 个 Python 文件与源码集合和字节一致；69 个 npm 包文件与当前源码/构建输出逐字节匹配。A 包 SHA256 为 `a21007d6f554e265d3165ac38a4ad082875173d17ba07ef307ce44ef43b1d369`。
- B 最终 ZIP 解包的 122 个文件均与打包清单一致；ZIP 经实际 Windows 安装器安装后，上述 10 项原生检查再次通过。最终已安装程序 SHA256 为 `fcbda3f3a9d22fa34b05b9c6e7c73a1b28d4850847ffbb6e6eeec39ad606e860`。
- B ZIP SHA256 为 `e0fd709c0ecf01a0ab0c2aa6fce0f1c4f63ade6a711b9067569163902e002292`；其余交付 SHA 见 rc.2 目录内清单。

可复核证据：A `outputs/v0.2/reading-assistant/验收.json` 与四张界面截图；B `outputs/model-policy-native.json`；A 包审计在 B `inputs/A-PACKAGE-AUDIT.json`。交付目录为 `D:/Vibe Coding/.v02-build/delivery-v0.2.0-rc.2`。使用方法见 [v0.2 使用指南](v0.2-guide.md)。

模型验收使用本地合成适配器，仅证明真实宿主和请求路由，不证明实际 Luna/Sol 的翻译或科学回答质量。移动端使用 390px 浏览器模拟，未做 iOS/Android 实机验收；实际模型账号、OAuth 和公网部署不在本次运行验证内。

## rc.1 历史记录

更新：2026-09-09。五类功能的工程实现、迁移和 Windows 候选验收已完成。版本为 0.2.0-rc.1，交付可审查的工作树快照；尚未提交、合并或正式发布。

## 工作区和事实源

A 工作区为 D:/Vibe Coding/.v02-build/dsh-scientific-reading，基线 bd35fab9cc684e4f56330457a6430aeadb4e0331；B 为同级 codex-scientific-reading，基线 65f2eef6a611565fd90b66112571cb3e286af8c1。两者分支均为 codex/v0.2-build。基线 commit 不代表最终工作树内容。

SQLite 保存论文身份、会话绑定、研究字段、方案、模板和雷达；DSH 保存原生历史。Excel 是派生视图，按稳定 paperId/fieldId 有限回写。原 A 的 Reader 导读改动和 .gitattributes、原 B 的授权获取改动经审查整合，原目录保留。原 A Reader SHA256 仍为 0D52278B31CE211708DD1F8B383DCA070E92ED1412CCFC29D423D48298B2645C；B 9 个整合来源文件与原始记录一致。

## 五类功能

| 功能 | 已交付行为 | 关键验收记录 |
| --- | --- | --- |
| 单篇 chat 和多篇历史 | paperId 唯一绑定原生 session；去重、移动、重启及原工作区续接；用户多选、来源、分页、截断和 SHA；旧分类及 Review 只读 | A native-chat.json；B native-handoff-final.json、b-features-final.json、upgrade-v02.json、legacy-history-ui.json |
| Figure 关联讨论 | 实际 PNG、图注、明确正文引用及邻段；子图和跨页；paperId/assetId/PDF SHA；换图更新原 chat；明确仅文字状态 | A/B 原生模型读取图片字节并核对 SHA；Python 图像、引用、过期身份与范围检查 |
| Excel 和研究字段 | 显隐、顺序、显示名、宽度、换行；全库/分类方案；人工和带证据 AI 字段；人工值保护与安全同步 | excel-browser-readback.json/.xlsx、excel-artifact-inspect.json、excel-research-preview.png；B 接口读回 |
| Reader 与主题 | 用户自选主题色；字体、字号、宽度及四种双语布局；HTML/CSS 导入、预览、应用、恢复；全库/分类方案 | reader-ui-acceptance.json、reader-dark-bilingual.png；B 原始/显示 SHA 验证 |
| 文献雷达 | 方向澄清配置、外发查询预览、用户确认、多源发现去重、四维解释及短引文、反馈、明确纳入、增量扫描及新相关提示 | radar-public-probe.json、radar-incremental-probe.json、radar-browser-readback.json、radar-assessment-acceptance.json、radar-admission-readback.json |

以上记录位于对应仓库 outputs/v0.2，关键证据另随交付汇集。操作见 [v0.2 使用指南](v0.2-guide.md)，B API 见其 skills/deep-literature-for-codex/references/v02.md。

## 最终检查

- A 完整 Python：550 passed、3 skipped。A 构建、完整 test:offline、test:assets 通过；启动探针后续调整另由 tests/acceptance-dsh.mjs 验证。
- A 真实 tarball 安装到独立 DSH 0.1.0-rc.7 Profile：11 项原生检查通过，包括已有 chat 的原工作区复用。
- B 完整 Node：82 passed、1 skipped。跳过项未计作通过。
- B 最终 ZIP 经解包和真实 Windows 安装器安装：原生交接 21 项、Figure/历史/显示/字段/雷达桥接 10 项通过，均记录同一最终 A 包和 B 程序 SHA。
- 独立 Chromium 的 600 KB 多段 PDF 传输、字节 SHA、HTML 拒绝和缓冲清理检查通过；仅使用本地合成站点及自建浏览器目录。
- A 轮包 60 个 Python 文件集合与字节匹配当前源码；61 个 npm 包文件逐一核对。最终保留原生验收所用 wheel，避免离线测试重建 ZIP 时间戳后误换交付物。
- B ZIP 121 个解包文件验证通过。BUILD-MANIFEST.json 标记 working-tree snapshot，sourceCommit 仅作基线。
- 最终 A 包 SHA256：ad094643cece9b3c5fdea55af32ea37e17091015e1bd84ad2a78f2cd5d41989d。
- 最终 B ZIP SHA256：3a9d0c4f41a5329186029dbc2cc2f3bc94bc597a2599ba21b2c4aede0d89242a。
- B 已安装程序 SHA256：c3dd230d6d10612402b59210c8880e2cc6ccc2a065d71b07cc03a474ef198485。

最终产物在两仓库 outputs/v0.2/release-candidate。更早 candidate、candidate-fixed、final 目录保留为历史验收产物，不作为本次交付。

## 迁移、界面与资产保留

真实安装的 v0.1.0-rc.8 生成旧库、Reader、Excel、笔记和原生混合讨论，再由候选安装器迁移格式 5 到 6。12 项检查通过：6 个 PDF/HTML/XLSX 字节不变，旧 handoff 原样备份，混合讨论保留原身份及只读正文，新单篇 chat 独立稳定，旧 Reader 的显示响应补入 Figure 控件而不重写文件。最后将最终候选装入同一升级实例，3 项读回再次确认旧资产、旧历史和 Reader。浏览器确认只读历史原文、混合来源提示和完整页状态，见 legacy-history-ui.png。

迁移代码支持格式 4/5 到 6；格式 6 不直接回退旧引擎。安装器已验证拒绝不兼容回退。需要旧版时，在独立目录恢复升级前完整备份。

Reader UI 实际完成 HTML/CSS 文件选择、深色双栏预览、保存应用、外部资源拒绝及恢复默认；35 个原始资产 SHA 不变。Excel 成品由 openpyxl 独立读取，再用 artifact-tool 导入、渲染并目视核对。

## 雷达与模型证据边界

Europe PMC、Crossref 完成真实公开接口发现和连续两页增量探针。浏览器先确认方向，再发现 22 条候选，此时正式库仍是 3 篇合成论文。人工反馈、带摘要短引文的四维评估均读回；明确点击纳入后才新增对应正式论文。摘要初筛不证明全文质量或病例因果关系。

初次查询有数量上限；增量保存窗口和每页游标，失败来源不推进。频率默认关闭，仅在用户保存频率且宿主运行时扫描，未完成分页按分钟续接；仅提示新的、相关评估仍有效的候选。本次验收频率为 0，未为用户创建实际定时任务。

工程模型为本地合成适配器：验证宿主工具和真实附件字节，不评价真实模型的科学回答。真实 OAuth、MinerU 付费服务、学校机构登录及全文权益未做账号验收。macOS/Linux 未做实机验收，本次交付 Windows x64 包。旧版式夹具中的 SVG 只用于显示/迁移检查；传图验收用真实 PNG，不声称支持 SVG 光栅化。

## 已修复的验收问题

原生 tools.guard 改为同步合同，异步权限校验放在执行环节；子会话读取可信父绑定，再由 SQLite 核验论文。Reader 和 Codex 重开同一 chat 时沿用原生工作区。CSS 支持合法子选择器/媒体比较并拒绝外部资源及样式闭合标签。CSRF、深色对比度、旧 Reader 控件和图像状态均经实际路径复验。启动探针原 600 毫秒会误报正常的 669 毫秒响应，单次调整为 5 秒，整体保留 30 秒启动期限。

A 自动获取仅限 OA PDF 或本地文件；B 按用户已有授权进行浏览器交接，不读取凭据或登录文件。逐篇 JSON 证据卡不在本次范围。正式发布和生产账号验收另行进行。
